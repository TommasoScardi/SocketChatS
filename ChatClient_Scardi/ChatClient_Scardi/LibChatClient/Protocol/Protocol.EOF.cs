namespace LibChatClient.Protocol
{
    /// <summary>
    /// Insieme di tutte le costanti relative al protocollo EOF
    /// </summary>
    public struct EOF
    {
        /// <summary>
        /// Equivalente in stringa dell Enumerazione Protocols di EOF (99)
        /// </summary>
        public const string ProtocolString = "<EOF>";
    }
}
